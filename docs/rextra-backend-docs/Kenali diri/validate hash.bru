meta {
  name: validate hash
  type: http
  seq: 1
}

post {
  url: {{host}}/api/v1/assesment/validate_hash
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "hash": ""
  }
}

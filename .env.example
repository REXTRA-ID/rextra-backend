# =========== (APP) ===========
APP_HOST=0.0.0.0
APP_PORT=8000
APP_MODE=development # or production
APP_NAME=Rextra
APP_VERSION=1.0.0
APP_URL=localhost
NGINX_PORT=80

# =========== (DATABASE) ===========
DB_HOST = localhost
DB_USER = postgres
DB_PASS = password
DB_NAME = db_name
DB_PORT = 5432

# =========== (JWT SECRET) ===========
JWT_SECRET = secret

# =========== (MAILER) ===========
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_NAME=
SMTP_AUTH_EMAIL=
SMTP_AUTH_PASSWORD=

# =========== (FIREBASE) ===========
FIREBASE_SERVICE_ACCOUNT_KEY=

# =========== (XENDIT) ===========
XENDIT_SECRET_KEY=